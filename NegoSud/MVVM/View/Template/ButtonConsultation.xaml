<Button x:Class="NegoSud.MVVM.View.Template.ButtonConsultation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Background="{StaticResource MainColorBrush}" Foreground="{StaticResource SecondColorBrush}" BorderBrush="Transparent" BorderThickness="0">
    
    <!--<Grid>

        <Button x:Name="ButtonView"  Click="ButtonView_Click"
                MouseEnter="ButtonAdd_MouseEnter" MouseLeave="ButtonAdd_MouseLeave">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>-->
            <materialDesign:PackIcon Kind="Visibility" VerticalAlignment="Center"/>
        <!--</Button>
    </Grid>-->
</Button>
